{
  "name": "fabot-sdk-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "rspress dev",
    "build": "rspress build && node scripts/rewrite-asset-paths.mjs",
    "preview": "rspress preview"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "devDependencies": {
    "@rspress/core": "^2.0.12",
    "rspress-plugin-mermaid": "^1.0.1"
  }
}
